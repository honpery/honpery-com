import { Mutation, Query, Type, types } from '../helpers';
import { User } from './user';

export const LoginMutation = Mutation('login', {

});

export const RegisterMutation = Mutation('register', {

});

export const VistorQuery = Query('vistor', {

});
